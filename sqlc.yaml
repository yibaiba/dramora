version: "2"
sql:
  - engine: postgresql
    queries: db/queries
    schema: db/migrations
    gen:
      go:
        package: repoqueries
        out: internal/repo/queries
        sql_package: pgx/v5
